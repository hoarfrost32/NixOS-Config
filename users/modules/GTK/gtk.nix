{ ... }:

{
  gtk = {
    enable = true;
    catppuccin = {
      enable = true;
      icon.enable = true;
      flavor = "mocha";
      accent = "lavender";
      size = "standard";
      tweaks = [ "normal" ];
    };
  };
}